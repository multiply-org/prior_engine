about:
  summary: "MULTIPLY prior engine package"
  home: https://github.com/multiply-org/prior-engine
  license: GPLv3

package:
  name: multiply_prior_engine
  version: "0.5.1"

source:
  git_url: "https://github.com/multiply-org/prior-engine.git"

build:
  number: 0

requirements:
  build:
    - python
    - setuptools

  run:
    - python
